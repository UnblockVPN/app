#include "Base.xcconfig"

// Provisioning profiles
// Comment out the lines below if using automatic code sign
// PROVISIONING_PROFILE_SPECIFIER[config=Debug][sdk=*][arch=*] = UnblockVPN Development
// PROVISIONING_PROFILE_SPECIFIER[config=Release][sdk=*][arch=*] = UnblockVPN Release
